# 最不容易被检测的自动化方案

## 核心结论：三层架构 - 外部读取 + 网络代理 + 输入模拟

当前项目的自动化操作本质上只有三种方式：
1. **直接发送网络封包**（战斗、聊天、移动等）
2. **调用游戏内部函数**（NPC对话框、UI操作等）
3. **修改游戏内存变量**（坐标、速度等）

最安全的方案是：**完全不注入游戏进程**，而是通过以下三层实现：

---

## 第一层：外部进程读取数据（零风险）

使用 `ReadProcessMemory` 从外部读取游戏状态。

**检测风险：几乎为零**

20多年前的游戏不太可能检测 `OpenProcess` + `ReadProcessMemory`，
因为这和调试器、性能监控等合法软件的行为完全一致。

---

## 第二层：网络代理执行操作（零注入风险）

这是最关键的创新点。当前项目的所有操作最终都是发送网络封包：

| 操作 | 封包格式 | 来源代码位置 |
|------|---------|-------------|
| 普通攻击 | `"H\|目标ID"` | gameservice.cpp:5318 |
| 技能攻击 | `"S\|技能位\|等级\|目标"` | gameservice.cpp:5356 |
| 防御 | `"G"` | gameservice.cpp:5482 |
| 逃跑 | `"E"` | gameservice.cpp:5504 |
| 复活 | `"R\|0"` 或 `"R\|1"` | gameservice.cpp:5459 |
| 聊天 | `"P\|消息内容"` | gameservice.cpp:6207 |
| 移动 | 方向字符 + 坐标 | gameservice.cpp:6953 |

**核心思路：在网络层拦截并注入封包，不需要注入游戏进程。**

### 实现方式：本地代理（Proxy）

```
游戏客户端 ←→ 本地代理(127.0.0.1:代理端口) ←→ 游戏服务器
                    ↑
              自动化脚本
           (可以读取和注入封包)
```

#### 工作流程：
1. 修改游戏配置文件或hosts文件，让游戏连接到本地代理
2. 代理转发游戏和服务器之间的所有封包
3. 自动化脚本通过代理：
   - **读取**服务器发来的封包（获取战斗状态、NPC对话等）
   - **注入**客户端发来的封包（执行战斗操作、移动等）

#### 优势：
- ✅ 游戏进程中没有任何DLL
- ✅ 没有内存修改
- ✅ 没有API Hook
- ✅ 从游戏角度看，封包就是正常网络流量
- ✅ 可以完全解析游戏协议

#### 需要解决的技术问题：
1. 找到游戏服务器的IP和端口
2. 解析游戏网络协议（从当前项目已有参考）
3. 处理封包加密（如果有）

---

## 第三层：输入模拟（零注入风险，用于UI操作）

对于无法通过网络封包完成的操作（如点击NPC对话框选项），
使用 Windows API 模拟用户输入。

### 方式一：SendMessage（推荐，最安全）
```cpp
// 模拟鼠标点击
SendMessage(hGameWnd, WM_LBUTTONDOWN, MK_LBUTTON, MAKELPARAM(x, y));
SendMessage(hGameWnd, WM_LBUTTONUP, 0, MAKELPARAM(x, y));

// 模拟键盘
SendMessage(hGameWnd, WM_KEYDOWN, VK_SPACE, 0);
SendMessage(hGameWnd, WM_KEYUP, VK_SPACE, 0);
```

### 方式二：SendInput（更真实，但稍复杂）
```cpp
INPUT input = {0};
input.type = INPUT_MOUSE;
input.mi.dx = X * (65535 / screenWidth);
input.mi.dy = Y * (65535 / screenHeight);
input.mi.dwFlags = MOUSEEVENTF_ABSOLUTE | MOUSEEVENTF_LEFTDOWN;
SendInput(1, &input, sizeof(INPUT));
```

### 方式三：PostMessage（异步，不阻塞）
```cpp
PostMessage(hGameWnd, WM_LBUTTONDOWN, MK_LBUTTON, MAKELPARAM(x, y));
PostMessage(hGameWnd, WM_LBUTTONUP, 0, MAKELPARAM(x, y));
```

**检测风险：极低**

20多年前的游戏几乎不可能区分 SendMessage 发来的消息和真实用户输入。
即使是现代游戏，检测 SendInput 也非常困难。

---

## 各操作的实现方案对照表

| 操作 | 当前项目方式 | 最安全方案 | 说明 |
|------|------------|-----------|------|
| 读取玩家信息 | 内存读取 | 外部ReadProcessMemory | 完全替代 |
| 读取战斗状态 | 内存读取 + 封包解析 | 外部ReadProcessMemory + 代理 | 完全替代 |
| 战斗-普通攻击 | 发送封包"H\|目标" | 网络代理注入封包 | 完全替代 |
| 战斗-技能攻击 | 发送封包"S\|..." | 网络代理注入封包 | 完全替代 |
| 战斗-防御 | 发送封包"G" | 网络代理注入封包 | 完全替代 |
| 战斗-逃跑 | 发送封包"E" | 网络代理注入封包 | 完全替代 |
| 战斗-复活 | 发送封包"R\|..." | 网络代理注入封包 | 完全替代 |
| 移动-行走 | 修改内存+发送封包 | 网络代理注入封包 | 需要额外处理客户端显示 |
| 移动-强制移动 | 修改坐标+发送封包 | 网络代理注入封包 | 同上 |
| 聊天 | 发送封包"P\|..." | 网络代理注入封包 | 完全替代 |
| NPC对话框 | 调用游戏内部函数 | 输入模拟(SendMessage) | 需要计算按钮位置 |
| 购买/出售 | 调用游戏内部函数 | 输入模拟(SendMessage) | 需要计算按钮位置 |
| 使用物品 | 发送封包 | 网络代理注入封包 | 完全替代 |
| 丢弃物品 | 发送封包 | 网络代理注入封包 | 完全替代 |
| 工作/采集 | 调用游戏内部函数 | 输入模拟(SendMessage) | 需要模拟UI操作 |
| 制作物品 | 调用游戏内部函数 | 输入模拟(SendMessage) | 需要模拟UI操作 |

---

## 网络代理的详细实现

### 第一步：找到游戏服务器连接信息

从 gameservice.h 中可以看到：
```cpp
char *g_game_server_ip;
char *g_game_server_port;
```

或者从 gameservice.cpp 中的网络函数：
```cpp
void(__fastcall *vce_connect)(void *pthis, int, const char *ipaddr, u_short port);
```

### 第二步：实现代理服务器

```cpp
// 代理服务器核心逻辑（伪代码）
class GameProxy {
    SOCKET m_clientSock;   // 与游戏客户端的连接
    SOCKET m_serverSock;   // 与游戏服务器的连接
    
    void Run() {
        // 1. 监听本地端口（游戏原来连接的端口）
        Listen(gamePort);
        
        // 2. 接受游戏客户端连接
        m_clientSock = Accept();
        
        // 3. 连接到真实游戏服务器
        m_serverSock = Connect(realServerIP, realServerPort);
        
        // 4. 双向转发 + 封包处理
        while (running) {
            fd_set fds;
            FD_ZERO(&fds);
            FD_SET(m_clientSock, &fds);
            FD_SET(m_serverSock, &fds);
            
            select(0, &fds, NULL, NULL, NULL);
            
            // 客户端 → 服务器（上行封包）
            if (FD_ISSET(m_clientSock, &fds)) {
                char buf[4096];
                int len = recv(m_clientSock, buf, sizeof(buf), 0);
                if (len > 0) {
                    OnUpstreamPacket(buf, len);
                    send(m_serverSock, buf, len, 0);
                }
            }
            
            // 服务器 → 客户端（下行封包）
            if (FD_ISSET(m_serverSock, &fds)) {
                char buf[4096];
                int len = recv(m_serverSock, buf, sizeof(buf), 0);
                if (len > 0) {
                    OnDownstreamPacket(buf, len);
                    send(m_clientSock, buf, len, 0);
                }
            }
        }
    }
    
    // 处理上行封包（客户端发给服务器的）
    void OnUpstreamPacket(char* buf, int len) {
        // 记录封包，用于理解协议
        LogPacket("UP", buf, len);
    }
    
    // 处理下行封包（服务器发给客户端的）
    void OnDownstreamPacket(char* buf, int len) {
        // 记录封包
        LogPacket("DOWN", buf, len);
        
        // 解析战斗状态等信息
        ParseGamePacket(buf, len);
    }
    
    // 注入封包（自动化脚本调用）
    void InjectPacket(const char* data, int len) {
        // 伪装成客户端发送
        send(m_serverSock, data, len, 0);
    }
};
```

### 第三步：让游戏连接到代理

有几种方式：

1. **修改hosts文件**（如果游戏使用域名连接）
2. **修改游戏配置文件**（如果有服务器地址配置）
3. **使用Proxifier等工具**强制代理
4. **修改游戏内存中的服务器地址**（用WriteProcessMemory，一次性修改）

---

## 完整的自动化脚本架构

```
┌────────────────────────────────────────────────────────────┐
│                    自动化脚本 (Python/JS)                    │
│  - 战斗逻辑                                                 │
│  - 寻路算法                                                 │
│  - 任务流程                                                 │
└──────────┬──────────────────────┬──────────────────────────┘
           │                      │
           ▼                      ▼
┌─────────────────────┐  ┌──────────────────────┐
│   网络代理模块       │  │   数据读取模块        │
│  - 封包转发          │  │  - ReadProcessMemory  │
│  - 封包注入          │  │  - 状态监控           │
│  - 封包解析          │  │  - 事件检测           │
└──────────┬──────────┘  └──────────────────────┘
           │                      │
           ▼                      ▼
┌─────────────────────┐  ┌──────────────────────┐
│   游戏服务器         │  │   游戏进程            │
│                     │  │  (无任何注入)          │
└─────────────────────┘  └──────────────────────┘

另外：输入模拟模块（SendMessage）→ 游戏窗口（用于UI操作）
```

---

## 对比：当前方案 vs 推荐方案

| 维度 | 当前项目方案 | 推荐方案 |
|------|------------|---------|
| DLL注入 | ✅ 需要 | ❌ 不需要 |
| API Hook | ✅ Detours | ❌ 不需要 |
| 窗口子类化 | ✅ 替换WNDPROC | ❌ 不需要 |
| 内存修改 | ✅ 修改坐标/速度 | ❌ 不修改 |
| 内存读取 | ✅ 进程内直接读 | ✅ 外部ReadProcessMemory |
| 操作执行 | ✅ 调用内部函数 | ✅ 网络封包+输入模拟 |
| 创建线程 | ✅ 在游戏进程内 | ❌ 不需要 |
| 修改窗口标题 | ✅ 添加CGA标识 | ❌ 不修改 |
| 检测风险 | 🔴 极高 | 🟢 极低 |

---

## 实施步骤

### 阶段一：数据读取（1-2天）
1. 从 gameservice.h 提取所有偏移
2. 实现外部 ReadProcessMemory 读取
3. 验证所有数据结构正确

### 阶段二：网络代理（3-5天）
1. 抓包分析游戏网络协议
2. 实现本地代理服务器
3. 解析关键封包格式
4. 实现封包注入功能

### 阶段三：输入模拟（1-2天）
1. 分析游戏UI布局
2. 实现 SendMessage 模拟点击
3. 实现键盘模拟

### 阶段四：脚本引擎（3-5天）
1. 选择脚本语言（推荐Python或Node.js）
2. 实现脚本API
3. 编写示例脚本

---

## 移动操作的特殊处理

移动是当前项目中最复杂的操作，因为它同时修改了内存和发送封包：

```cpp
// 当前项目的ForceMoveTo实现
g_player_xpos->set(x);        // 修改内存中的坐标
g_player_ypos->set(y);        // 修改内存中的坐标
NET_WriteMovePacket_cgitem(x, y, buf);  // 发送移动封包
*g_map_xf = x * 64.0f;       // 修改显示坐标
```

**网络代理方案中**：只发送移动封包，不修改内存。
游戏客户端会自己更新坐标和显示。

**但如果需要瞬间移动（穿墙等）**：
这是唯一需要修改内存的场景，可以用 `WriteProcessMemory` 从外部修改，
风险比注入DLL低得多，但仍需谨慎。

---

## 最终建议

对于你的场景（自己的游戏、20多年前的代码、有检测逻辑）：

1. **优先使用网络代理方案** - 这是最安全且功能最完整的方案
2. **数据读取用外部ReadProcessMemory** - 零风险
3. **UI操作用SendMessage** - 几乎零风险
4. **绝对不要注入DLL** - 这是被检测的最大风险点
5. **绝对不要修改游戏进程内存** - 除非万不得已

这套方案的开发工作量比直接注入大一些，但安全性是质的提升。
