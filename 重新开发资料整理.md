# CGAssistant 重新开发资料整理

如果你想要重新开发或深入理解这个项目，以下是你需要了解和准备的资料和技术。

## 一、开发环境准备

### 1.1 硬件要求
- **操作系统**：Windows 7 或更高版本（建议 Windows 10/11）
- **处理器**：支持 x86 架构（项目主要为 32 位）
- **内存**：至少 4GB RAM
- **硬盘**：至少 10GB 可用空间

### 1.2 软件要求（按顺序安装）

#### 1. Visual Studio 2019
- **下载地址**：https://visualstudio.microsoft.com/
- **需要组件**：
  - 使用 C++ 的桌面开发 (Desktop development with C++)
  - vc142 工具集 (MSVC v142 - VS 2019 C++ x86/x64)
  - Windows SDK (建议 Windows 10 SDK 或更高)
- **注意**：必须安装 32 位 (x86) 编译工具

#### 2. Qt 5.15 (32位)
- **下载地址**：https://www.qt.io/download
- **版本要求**：Qt 5.12 或 Qt 5.15
- **架构**：MSVC 2017/2019 32-bit (x86)
- **组件**：
  - Qt 5.15.x (MSVC 2019 32-bit)
  - Qt Creator (可选，用于开发界面)
- **环境变量配置**：
  - 添加 `Qt5.x.x\5.x.x\msvc2019\bin` 到 PATH
  - 添加 `Qt5.x.x\Tools\QtCreator\bin` 到 PATH

#### 3. Node.js v20.11.1 LTS (32位)
- **下载地址**：https://nodejs.org/dist/v20.11.1/
- **文件名**：node-v20.11.1-x86.msi (Windows 32位)
- **验证安装**：
  ```cmd
  node -v
  npm -v
  ```

#### 4. node-gyp
- **安装命令**：
  ```cmd
  npm install -g node-gyp
  ```
- **依赖**：需要 Python (通常 Node.js 安装时会自动安装)

#### 5. Boost 库 (需要编译)
- **下载地址**：https://www.boost.org/
- **推荐版本**：Boost 1.7x 或更高
- **编译步骤**：
  1. 解压 Boost 源码
  2. 打开 VS2019 x86 本机工具命令提示符
  3. 运行引导程序：
     ```cmd
     bootstrap.bat
     ```
  4. 编译 Boost (32位静态库)：
     ```cmd
     b2 --build-dir=build\x86 address-model=32 --stagedir=stage\x86 stage --build-type=complete
     ```
- 或使用项目提供的 `build-boost.bat` 脚本

### 1.3 项目初始化
1. 克隆项目
2. 运行 `init-dep.bat` 初始化子模块
3. 按顺序运行构建脚本：
   - `build-boost.bat`
   - `build-CGAHook.bat`
   - `build-qhttp.bat`
   - `build-CGAssistant.bat`
   - `build-nodegyp.bat`

## 二、核心技术栈学习路径

### 2.1 Windows 系统编程 (基础)
需要掌握的核心知识：

#### 推荐学习资源
- **书籍**：
  - 《Windows 程序设计》 (Charles Petzold) - 窗口消息机制基础
  - 《Windows 核心编程》 (Jeffrey Richter) - 进程、线程、内存管理
- **在线文档**：
  - Microsoft Docs: https://docs.microsoft.com/windows/win32/
- **视频教程**：
  - B站/YouTube 搜索 "Windows API 编程"

#### 需要掌握的关键知识点
1. **窗口消息机制**
   - 消息循环 (Message Loop)
   - 窗口过程 (Window Procedure)
   - 自定义消息 (WM_USER + x)
   - SendMessage/PostMessage 区别

2. **进程与线程**
   - CreateProcess/OpenProcess
   - CreateThread
   - 进程间通信 (IPC)

3. **内存管理**
   - VirtualAlloc/VirtualProtect
   - ReadProcessMemory/WriteProcessMemory
   - 内存分页保护机制

4. **DLL 相关**
   - DllMain
   - 动态链接库加载/卸载
   - 导出函数 (\_\_declspec(dllexport))

### 2.2 DLL 注入与 Hook 技术

#### Microsoft Detours 库
- **官方仓库**：https://github.com/microsoft/Detours
- **文档**：项目 `Detours` 目录下的文档
- **核心概念**：
  - Trampoline（跳板函数）
  - DetourTransaction（事务）
- **学习示例**：Detours 自带的样例代码

#### DLL 注入技术
- **SetWindowsHookEx** (项目使用方式)
- **CreateRemoteThread + LoadLibrary**
- **反射式注入** (Reflective DLL Injection)
- **推荐资源**：
  - 《Grey Hat Python》中的相关章节
  - 安全论坛的注入教程

### 2.3 逆向工程 (关键)
这是开发此类项目最核心的技能，需要找到游戏数据在内存中的位置：

#### 工具准备
1. **x64dbg/x32dbg** (调试器)
   - 下载：https://x64dbg.com/
2. **Cheat Engine** (内存扫描)
   - 下载：https://www.cheatengine.org/
3. **IDA Pro** 或 **Ghidra** (反汇编)
   - IDA：https://hex-rays.com/ida-pro/ (商业软件)
   - Ghidra：https://ghidra-sre.org/ (免费)
4. **API Monitor** (监控 API 调用)
   - http://www.rohitab.com/apimonitor

#### 学习路线
1. **基础调试**
   - 学习使用调试器设置断点
   - 查看内存、寄存器、调用栈
   - 单步执行 (step into/step over)

2. **内存搜索** (Cheat Engine)
   - 搜索已知数值 (如 HP、金币)
   - 找出什么访问/修改了这个地址
   - 找到静态基址 + 偏移

3. **反汇编分析**
   - 理解 x86 汇编基础
   - 识别函数调用约定 (cdecl/stdcall/thiscall)
   - 分析游戏关键函数

#### 关键技能
- **指针链追踪**：找出动态数据结构的访问路径
- **数据结构逆向**：从内存布局推断结构体定义
- **函数签名识别**：识别游戏内部函数的参数和返回值

### 2.4 C++ 开发
项目使用现代 C++，需要熟悉：

#### 语言特性
- C++11/14/17 特性
- STL 容器 (vector, string, map 等)
- 智能指针
- 函数对象与 Lambda
- 模板基础

#### 推荐资源
- 《C++ Primer》
- 《Effective C++》系列

### 2.5 Qt 框架
用于开发图形界面：

#### 核心模块
- **Qt Widgets**：传统桌面界面
- **Qt Core**：核心非 GUI 功能
- **Qt Network**：网络通信
- **Qt Script** (旧版)：脚本支持

#### 学习资源
- Qt 官方文档：https://doc.qt.io/
- 《C++ GUI Qt4 编程》

### 2.6 Node.js 与 NAN
用于实现脚本引擎：

#### NAN (Native Abstractions for Node.js)
- **GitHub**：https://github.com/nodejs/nan
- **用途**：简化 Node.js 原生模块开发，兼容不同 Node.js 版本
- **需要理解**：
  - V8 引擎基础
  - JS 值与 C++ 值的转换
  - 异步工作 (Nan::AsyncWorker)
  - 回调函数

#### node-gyp
- **构建系统**：生成 Visual Studio 项目
- **配置文件**：binding.gyp

#### 相关资源
- Node.js 官方 C++ 插件文档
- NAN 仓库中的示例代码

## 三、游戏数据结构分析 (最关键)

### 3.1 需要逆向的关键数据
以下是项目中需要定位和逆向的数据结构：

| 数据类别 | 需要的信息 | 用途 |
|---------|----------|------|
| 玩家信息 | HP/MP/等级/金币/坐标等 | 状态显示、条件判断 |
| 宠物信息 | 宠物属性、技能、状态 | 宠物管理、战斗 |
| 物品信息 | 物品ID、数量、位置 | 物品管理、自动使用 |
| 技能信息 | 技能列表、等级、消耗 | 战斗、工作 |
| 战斗系统 | 战斗单位列表、回合状态 | 自动战斗 |
| 地图信息 | 地图ID、碰撞表、坐标 | 自动寻路、移动 |
| NPC 对话 | 对话框内容、选项 | 自动任务、交互 |
| 网络封包 | 封包结构、加密方式 | 深入控制 (可选) |

### 3.2 逆向方法示例 (以玩家 HP 为例)
```
1. 在 Cheat Engine 中搜索当前 HP 值
2. 在游戏中改变 HP (如被攻击或吃药)
3. 再次搜索新的 HP 值，重复直到找到少数地址
4. 对找到的地址设置 "找出是什么访问了这个地址"
5. 在游戏中进行操作，查看汇编指令
6. 分析汇编，找到基址 + 偏移的访问模式
7. 在调试器中验证，确认结构
```

### 3.3 内存偏移维护
游戏每次更新，内存偏移可能会变化，需要维护：

```cpp
// 假设的偏移定义
struct GameOffsets {
    uintptr_t player_base;        // 玩家数据基址
    uintptr_t player_hp;          // HP 偏移
    uintptr_t player_mp;          // MP 偏移
    uintptr_t player_gold;        // 金币偏移
    uintptr_t battle_base;        // 战斗数据基址
    // ... 更多偏移
};
```

## 四、项目架构理解

### 4.1 模块划分
完全理解每个模块的职责：
- **CGALib**：接口定义、数据结构
- **cgahook**：注入 DLL、消息处理、游戏服务
- **CGAssistant**：Qt 界面、用户交互
- **CGANode**：Node.js 绑定、脚本引擎

### 4.2 通信流程
画图理解以下通信路径：
1. UI → CGAssistant → 自定义消息 → cgahook → 游戏
2. 游戏 → cgahook → 回调/通知 → CGAssistant → UI
3. JS 脚本 → Node.js → node_cga → CGAssistant → ...

### 4.3 线程安全
注意多线程问题：
- 窗口消息只在创建线程处理
- 游戏数据访问可能需要同步
- Node.js 回调的线程模型

## 五、调试与测试技巧

### 5.1 调试 cgahook.dll
因为是注入 DLL，调试比较特殊：

1. **使用调试器启动游戏**
   ```
   x32dbg.exe cg_item_6000.exe
   ```

2. **在 DllMain 设置断点**
   - 或者在注入后使用 `DebugBreak()`

3. **附加到进程**
   - 先运行游戏和辅助
   - 再用 VS 或调试器附加到游戏进程

### 5.2 日志输出
在 `cgahook` 中添加日志：
```cpp
// 使用 OutputDebugString
OutputDebugStringA("Debug message here\n");

// 或使用日志文件
FILE* log = fopen("cga_hook.log", "a");
fprintf(log, "[%s] %s\n", __FUNCTION__, message);
fclose(log);
```

### 5.3 查看消息
使用工具 Spy++ 或类似工具查看窗口消息。

## 六、学习项目源码的顺序

建议按以下顺序阅读代码：

1. **第一步：CGALib/gameinterface.h**
   - 理解所有数据结构定义
   - 了解提供了哪些功能接口

2. **第二步：cgahook/main.cpp**
   - 理解 DLL 如何注入
   - 理解窗口过程子类化
   - 看到所有自定义消息的处理

3. **第三步：CGALib/gameinterface.cpp** (如果存在)
   - 看接口如何实现
   - 看如何访问游戏内存

4. **第四步：CGAssistant 源码**
   - 看 UI 如何组织
   - 看如何与 cgahook 通信

5. **第五步：CGANode 源码**
   - 理解 JS/C++ 绑定
   - 理解异步工作处理

## 七、常见问题与坑

### 7.1 32位 vs 64位
- 游戏是 32位的，所有组件都必须是 32位
- 不要混用 64位库或工具

### 7.2 管理员权限
- 注入和进程操作可能需要管理员权限
- 以管理员身份运行程序

### 7.3 杀毒软件误报
- DLL 注入和 API Hook 可能被杀毒软件标记
- 需要添加信任或临时关闭杀毒

### 7.4 游戏更新维护
- 每次游戏更新后可能需要重新找偏移
- 建议设计偏移配置文件，方便更新

### 7.5 崩溃问题
- 访问无效内存地址是常见崩溃原因
- 添加大量的空指针检查和边界检查
- 实现异常处理 (SEH/VEH)

## 八、进阶扩展方向

如果你想在现有基础上扩展，可以考虑：

### 8.1 功能扩展
- 添加更多游戏功能支持
- 支持更多游戏版本
- 添加自动任务脚本系统

### 8.2 技术升级
- 升级到 Qt 6
- 升级到更新的 Node.js
- 使用更现代的 C++ (C++20)

### 8.3 安全加固
- 代码混淆 (如果需要发布)
- 反调试 (对抗游戏保护)
- 虚拟机检测 (可选)

### 8.4 性能优化
- 减少不必要的内存读取
- 使用内存缓存
- 优化通信频率

## 九、参考项目与社区

### 9.1 类似项目
- 搜索 GitHub 上的类似游戏辅助项目
- 学习不同的实现方式

### 9.2 社区资源
- **看雪论坛**：https://bbs.pediy.com/ (国内逆向安全社区)
- **吾爱破解**：https://www.52pojie.cn/
- **GitHub**：搜索相关关键词

### 9.3 原项目资源
- **原项目仓库**：https://github.com/hzqst/CGAssistant
- **脚本仓库**：https://github.com/hzqst/CGAssistantJS
- **Wiki 文档**：项目 GitHub Wiki

## 十、法律与道德提醒

### 10.1 重要声明
- 此类工具仅用于学习交流
- 不要用于商业用途
- 不要发布到 GitHub 以外的平台
- 使用风险自负，可能导致账号封禁

### 10.2 许可证
- 项目使用 MIT 许可证
- 但项目特殊声明可能优先于 MIT

---

**祝开发顺利！如果遇到问题，可以查看原项目的 Issue 和 PR，可能已有类似问题的解决方案。**
