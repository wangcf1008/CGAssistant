# CGAssistant 自动辅助原理解析

## 核心原理概述

CGAssistant 采用**进程注入 + API Hook** 的技术方案，实现对魔力宝贝游戏的深度集成和自动化控制。这种方案不需要游戏官方提供任何公开接口，而是直接与游戏进程内存和消息系统交互。

## 1. DLL 注入机制

### 1.1 注入方式

项目主要使用以下几种方式将 `cgahook.dll` 注入到游戏进程：

#### 窗口消息钩子 (GetMsgProc)
```cpp
// cgahook/main.cpp 中的导出函数
__declspec(dllexport) LRESULT WINAPI GetMsgProc(int Code, WPARAM wParam, LPARAM lParam)
{
    PMSG pMsg = (PMSG)lParam;
    if (Code == HC_ACTION && pMsg->hwnd != NULL && !g_MainHwnd)
    {
        // 检测目标游戏窗口
        char szClass[256];
        if (GetClassNameA(pMsg->hwnd, szClass, 256))
        {
            WCHAR szModulePath[MAX_PATH];
            GetModuleFileNameW(NULL, szModulePath, MAX_PATH);
            LPCWSTR pModuleName = ExtractFileName(szModulePath);
            
            // 识别不同版本的游戏进程
            if (!_wcsicmp(pModuleName, L"cg_item_6000.exe") && !strcmp(szClass, "魔力宝贝"))
            {
                InitializeHooks(GetCurrentProcessId(), GetCurrentThreadId(), pMsg->hwnd, CGA::cg_item_6000);
            }
            else if (!_wcsicmp(pModuleName, L"POLCN_Launcher.exe"))
            {
                InitializeHooks(GetCurrentProcessId(), GetCurrentThreadId(), pMsg->hwnd, CGA::polcn);
            }
        }
    }
    return CallNextHookEx(NULL, Code, wParam, lParam);
}
```

#### DLL 入口点检测
```cpp
int WINAPI DllMain(HINSTANCE hinstDLL, DWORD fdwReason, LPVOID lpReserved)
{
    if (fdwReason == DLL_PROCESS_ATTACH)
    {
        WCHAR szModulePath[MAX_PATH];
        GetModuleFileNameW(NULL, szModulePath, MAX_PATH);
        LPCWSTR pModuleName = ExtractFileName(szModulePath);

        g_hCGAModule = (HMODULE)hinstDLL;
        GetModuleFileNameW(g_hCGAModule, g_szCGAModulePath, 1024);

        // 检测是否是目标进程
        if (!_wcsicmp(pModuleName, L"POLCN_Launcher.exe"))
        {
            // 初始化钩子
            InitializeHooks(GetCurrentProcessId(), GetCurrentThreadId(), NULL, CGA::polcn);
        }
        else if (!_wcsicmp(pModuleName, L"cg_se_6000.exe"))
        {
            // 特定版本游戏的额外补丁
            // ...
        }
    }
    return TRUE;
}
```

### 1.2 钩子初始化过程

```cpp
void InitializeHooks(ULONG ProcessId, ULONG ThreadId, HWND hWnd, CGA::game_type type)
{
    if (g_MainThreadId && g_MainThreadId != ThreadId)
        return;

    g_MainProcessId = ProcessId;
    g_MainThreadId = ThreadId;
    g_MainHwnd = hWnd;

    // 设置异常处理
    SetUnhandledExceptionFilter(MinidumpCallback);

    // 初始化游戏服务
    g_CGAService.Initialize(type);

    // 替换窗口过程
    if (hWnd && !g_OldProc)
    {
        g_OldProc = (WNDPROC)GetWindowLongPtrA(hWnd, GWL_WNDPROC);
        SetWindowLongPtrA(hWnd, GWL_WNDPROC, (LONG_PTR)(type == CGA::game_type::polcn ? NewWndProcPOLCN : NewWndProc));

        // 启动服务线程
        g_hServreThread = CreateThread(NULL, 0, CGAServerThread, NULL, 0, NULL);
    }
}
```

## 2. 窗口过程子类化 (Subclassing)

这是项目的核心技术之一，通过替换游戏窗口的消息处理函数来拦截和处理自定义命令：

### 2.1 自定义窗口过程

```cpp
LRESULT CALLBACK NewWndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam)
{
    switch (message)
    {
    case WM_CLOSE:
        // 处理窗口关闭，清理资源
        // ...
        break;
        
    case WM_SETTEXT:
        // 更新窗口标题，显示辅助信息
        if (g_MainPort)
        {
            strcpy(g_BasicWindowTitle, (const char *)lParam);
            return UpdateGameWindowTitle();
        }
        break;
        
    // ==============================
    // 自定义消息处理 (以 WM_CGA_ 开头)
    // ==============================
    
    case WM_CGA_BATTLE_NORMALATTACK:
        return g_CGAService.WM_BattleNormalAttack(wParam) ? 1 : 0;
        
    case WM_CGA_BATTLE_SKILLATTACK:
        return g_CGAService.WM_BattleSkillAttack((wParam & 0xFF), (wParam >> 8), lParam) ? 1 : 0;
        
    case WM_CGA_GET_PLAYER_INFO:
        g_CGAService.WM_GetPlayerInfo((CGA::cga_player_info_t *)wParam);
        return 1;
        
    // ... 更多自定义消息处理
        
    case WM_KEYDOWN:
        // 处理快捷键
        if (GetForegroundWindow() == g_MainHwnd)
        {
            switch (wParam)
            {
            case VK_F1:
                g_CGAService.WM_LogBack();
                return 1;
            case VK_F2:
                g_CGAService.WM_LogOut();
                return 1;
            // ... 更多快捷键
            }
        }
    }

    // 调用原始窗口过程
    return CallWindowProcA(g_OldProc, hWnd, message, wParam, lParam);
}
```

### 2.2 自定义消息的作用

通过定义一系列 `WM_CGA_*` 消息，实现从外部进程向游戏进程发送命令：

| 消息类型 | 功能 |
|---------|------|
| WM_CGA_BATTLE_* | 战斗相关操作 |
| WM_CGA_GET_* | 获取游戏信息 |
| WM_CGA_MOVE_* | 移动相关操作 |
| WM_CGA_USE_ITEM | 使用物品 |
| WM_CGA_DROP_* | 丢弃物品/宠物 |
| WM_CGA_CLICK_NPC_DIALOG | 点击 NPC 对话框 |
| WM_CGA_SAY_WORDS | 发送聊天消息 |
| WM_CGA_START_WORK | 开始工作（采集/制作） |
| WM_CGA_CRAFT_ITEM | 制作物品 |
| WM_CGA_LOG_* | 登出/登回 |

## 3. 游戏数据访问

### 3.1 内存操作

`CGAService` 类负责直接访问游戏进程内存，读取和写入游戏数据：

- **静态地址扫描**：游戏中一些关键数据结构位于固定的内存偏移处
- **动态地址追踪**：通过指针链追踪动态分配的数据结构
- **数据结构解析**：将原始内存数据解析为有意义的结构（如 `cga_player_info_t`）

### 3.2 数据结构定义

在 `CGALib/gameinterface.h` 中定义了大量数据结构，例如：

```cpp
typedef struct cga_player_info_s
{
    // 基础属性
    int hp;
    int maxhp;
    int mp;
    int maxmp;
    int xp;
    int maxxp;
    
    // 状态信息
    int health;
    int souls;
    int level;
    int gold;
    
    // 详细信息
    int avatar_id;
    int image_id;
    int unitid;
    int petid;
    
    // 字符串信息
    std::string name;
    std::string job;
    std::string nick;
    
    // 详细属性
    cga_playerpet_detail_info_t detail;
    
    // 其他信息
    std::vector<std::string> titles;
    cga_pers_desc_t persdesc;
} cga_player_info_t;
```

## 4. 跨进程通信机制

### 4.1 共享内存 (File Mapping)

```cpp
// 创建或打开共享内存
g_hFileMapping = CreateFileMappingA(INVALID_HANDLE_VALUE, NULL, PAGE_READWRITE, 0, sizeof(CGA::CGAShare_t), szName);

// 映射到进程地址空间
CGA::CGAShare_t* pShare = (CGA::CGAShare_t*)MapViewOfFile(g_hFileMapping, FILE_MAP_ALL_ACCESS, 0, 0, 0);

// 写入数据
pShare->ProcessId = g_MainProcessId;
pShare->ThreadId = g_MainThreadId;
pShare->hWnd = (int)g_MainHwnd;
pShare->Port = g_MainPort;
pShare->GameType = (int)g_CGAService.m_game_type;
```

### 4.2 窗口消息发送

外部进程通过 `SendMessage` 或 `PostMessage` 向游戏窗口发送自定义消息：

```cpp
// 示例：从外部进程获取玩家信息
CGA::cga_player_info_t playerInfo;
SendMessage(g_hGameWnd, WM_CGA_GET_PLAYER_INFO, (WPARAM)&playerInfo, 0);
```

## 5. 自动化功能实现

### 5.1 自动战斗系统

```cpp
// 战斗流程控制
// 1. 获取战斗单位信息
cga_battle_units_t units;
g_CGAService.WM_GetBattleUnits(&units);

// 2. 获取战斗上下文
cga_battle_context_t ctx;
g_CGAService.WM_GetBattleContext(&ctx);

// 3. 执行战斗操作
// 普通攻击
g_CGAService.WM_BattleNormalAttack(targetIndex);
// 技能攻击
g_CGAService.WM_BattleSkillAttack(skillIndex, skillLevel, targetIndex, packetOnly);
// 防御
g_CGAService.WM_BattleGuard();
// 逃跑
g_CGAService.WM_BattleEscape();
```

### 5.2 自动移动和寻路

```cpp
// 地图导航
// 1. 获取当前位置
int x, y;
g_CGAService.WM_GetMapXY(&x, &y);

// 2. 获取地图碰撞表
cga_map_cells_t cells;
g_CGAService.WM_GetMapCollisionTable(loadAll, &cells);

// 3. 移动到目标位置
g_CGAService.WM_ForceMoveTo(targetX, targetY, showAnimation);

// 或者使用普通行走
g_CGAService.WM_WalkTo(targetX, targetY);
```

### 5.3 自动工作/采集

```cpp
// 开始采集工作
g_CGAService.WM_StartWork(skillIndex, subSkillIndex);

// 制作物品
cga_craft_item_t craft;
craft.skill_index = skillIndex;
craft.subskill_index = subSkillIndex;
craft.sub_type = subType;
// 设置材料位置...
g_CGAService.WM_CraftItem(&craft);
```

### 5.4 NPC 交互自动化

```cpp
// 点击 NPC 对话框选项
g_CGAService.WM_ClickNPCDialog(optionIndex, dialogIndex);

// 购买物品
cga_buy_items_t items;
// 添加要购买的物品...
g_CGAService.WM_BuyNPCStore(&items);

// 出售物品
cga_sell_items_t items;
// 添加要出售的物品...
g_CGAService.WM_SellNPCStore(&items);
```

## 6. API Hook (Microsoft Detours)

项目使用 Microsoft Detours 库来 Hook 一些 Windows API：

### 6.1 为什么需要 API Hook

1. **修复游戏 Bug**：例如修复某些版本游戏的窗口化问题
2. **增强功能**：添加游戏本身没有的功能
3. **监控行为**：监控游戏的某些 API 调用

### 6.2 Hook 示例：CreateMutexA

```cpp
HANDLE WINAPI NewCreateMutexA_PatchGame(LPSECURITY_ATTRIBUTES lpMutexAttributes, BOOL bInitialOwner, LPCSTR lpName)
{
    if (!lpMutexAttributes && bInitialOwner == 1 && lpName)
    {
        // 修复 se_6000 窗口化 Bug
        if (g_CGAService.m_game_type == CGA::cg_se_6000)
        {
            // 检查调用栈
            bool bLegitCaller = false;
            PVOID callers[16];
            ULONG caller_count = RtlWalkFrameChain(callers, _countof(callers), 0);
            for (ULONG i = 0; i < caller_count; ++i)
            {
                if (callers[i] == (PVOID)(g_CGAService.m_ImageBase + 0x51F60))
                {
                    bLegitCaller = true;
                    break;
                }
            }
            
            // 修改游戏代码
            if (bLegitCaller)
            {
                DWORD oldProtect;
                VirtualProtect((void*)(g_CGAService.m_ImageBase + 0x3BAD3), 1, PAGE_EXECUTE_READWRITE, &oldProtect);
                memcpy((void*)(g_CGAService.m_ImageBase + 0x3BAD3), "\xEB", 1);
                VirtualProtect((void*)(g_CGAService.m_ImageBase + 0x3BAD3), 1, oldProtect, &oldProtect);
            }
        }
    }
    return g_pfnCreateMutexA(lpMutexAttributes, bInitialOwner, lpName);
}
```

### 6.3 Detours 的使用方式

```cpp
// 1. 保存原始函数指针
g_pfnCreateMutexA = (typeCreateMutexA *)GetProcAddress(GetModuleHandleA("kernel32.dll"), "CreateMutexA");

// 2. 开始事务
DetourTransactionBegin();

// 3. 附加 Hook
DetourAttach(&(void *&)g_pfnCreateMutexA, ::NewCreateMutexA_PatchGame);

// 4. 提交事务
DetourTransactionCommit();
```

## 7. 事件通知机制

项目实现了回调机制，当游戏中发生特定事件时通知外部程序：

```cpp
// 在 CGAInterface 中定义
virtual bool RegisterBattleActionNotify(const std::function<void(int)> &callback) = 0;
virtual bool RegisterNPCDialogNotify(const std::function<void(cga_npc_dialog_t)> &callback) = 0;
virtual bool RegisterChatMsgNotify(const std::function<void(cga_chat_msg_t)> &callback) = 0;
virtual bool RegisterWorkingResultNotify(const std::function<void(cga_working_result_t)> &callback) = 0;
// ... 更多通知
```

## 8. Node.js 脚本引擎

通过 Node.js 绑定，用户可以使用 JavaScript 编写自动化脚本：

### 8.1 绑定原理

使用 `nan` (Native Abstractions for Node.js) 库将 C++ 接口暴露给 JavaScript：

```cpp
// CGANode/main.cc - 简化示例
NAN_METHOD(GetPlayerInfo)
{
    // 获取 JavaScript 参数
    // ...
    
    // 调用 C++ 接口
    CGA::cga_player_info_t info;
    if (g_Interface->GetPlayerInfo(info))
    {
        // 转换为 JavaScript 对象
        v8::Local<v8::Object> obj = Nan::New<v8::Object>();
        Nan::Set(obj, Nan::New("name").ToLocalChecked(), Nan::New(info.name).ToLocalChecked());
        Nan::Set(obj, Nan::New("hp").ToLocalChecked(), Nan::New(info.hp));
        // ... 设置其他属性
        
        info.GetReturnValue().Set(obj);
    }
}

// 注册模块
NAN_MODULE_INIT(InitAll)
{
    Nan::Set(target, Nan::New("GetPlayerInfo").ToLocalChecked(),
        Nan::GetFunction(Nan::New<v8::FunctionTemplate>(GetPlayerInfo)).ToLocalChecked());
    // ... 注册其他函数
}

NODE_MODULE(node_cga, InitAll)
```

### 8.2 脚本示例

```javascript
// JavaScript 脚本示例
const cga = require('./build/Release/node_cga.node');

// 获取玩家信息
const player = cga.GetPlayerInfo();
console.log(`玩家: ${player.name}, HP: ${player.hp}/${player.maxhp}`);

// 自动战斗循环
cga.RegisterBattleActionNotify((action) => {
    if (action === 0) { // 轮到玩家行动
        // 使用技能攻击第一个敌人
        cga.BattleSkillAttack(0, 10, 0, false);
    }
});
```

## 9. 多进程架构总结

```
┌─────────────────────────────────────────────────────────┐
│                     用户脚本 (JS)                       │
└───────────────────────────┬─────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│                  Node.js 运行时                         │
│              (node_cga.node 插件)                       │
└───────────────────────────┬─────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│              CGAssistant.exe (Qt 界面)                  │
│         - 进程管理                                       │
│         - 界面交互                                       │
│         - 配置管理                                       │
└───────────────────────────┬─────────────────────────────┘
                            │
              ┌─────────────┴─────────────┐
              │  SendMessage/PostMessage  │
              │  Shared Memory            │
              └─────────────┬─────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│              游戏进程 (cg_*.exe)                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │          cgahook.dll (已注入)                      │  │
│  │  - 窗口过程子类化 (Subclassing)                    │  │
│  │  - API Hook (Detours)                              │  │
│  │  - CGAService (游戏数据访问)                        │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │              游戏原始代码                           │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## 10. 安全与风险

1. **内存安全**：直接操作游戏内存可能导致游戏崩溃
2. **账号安全**：此类辅助工具可能违反游戏服务条款
3. **稳定性**：游戏更新可能导致内存地址变化，需要维护
4. **兼容性**：不同游戏版本可能需要不同的偏移量

## 11. 技术总结

| 技术点 | 实现方式 | 用途 |
|-------|---------|------|
| DLL 注入 | GetMsgProc / DllMain | 进入游戏进程空间 |
| 窗口子类化 | SetWindowLongPtr | 拦截窗口消息，处理自定义命令 |
| API Hook | Microsoft Detours | 修复 Bug、增强功能 |
| 内存操作 | 直接指针访问 | 读写游戏数据 |
| 进程间通信 | WM_COPYDATA / 共享内存 / SendMessage | 组件间数据传输 |
| 脚本引擎 | Node.js + NAN | 允许用户用 JS 写脚本 |
