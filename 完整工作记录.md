# CGAssistant 项目分析与外部读取方案 - 完整记录

## 一、项目概述

CGAssistant 是一个用于游戏"魔力宝贝"(CrossGate)的开源自动化辅助程序。
- 仓库地址: https://github.com/hzqst/CGAssistant
- 技术栈: C++ / Qt 5 / Node.js / Microsoft Detours
- 核心原理: DLL注入 + API Hook + 窗口子类化

## 二、项目结构

### 核心模块

| 目录 | 功能 |
|------|------|
| CGALib/ | 核心接口定义(gameinterface.h)和数据结构 |
| cgahook/ | 注入DLL，包含main.cpp(注入入口)、gameservice.h/cpp(游戏数据访问) |
| CGAssistant/ | Qt GUI主程序 |
| CGANode/ | Node.js绑定，允许JS脚本 |
| Detours/ | Microsoft Detours API Hook库 |
| rest_rpc/ | RPC通信库 |

### 关键文件

- `CGALib/gameinterface.h`: 定义了所有数据结构(cga_player_info_t等)和接口(CGAInterface)
- `cgahook/main.cpp`: DLL入口，窗口子类化，自定义WM_CGA_*消息处理
- `cgahook/gameservice.h`: CGAService类，定义了游戏偏移量、Hook函数指针
- `cgahook/gameservice.cpp`: 核心实现，包含Initialize函数(设置偏移)和所有WM_*处理函数

## 三、原项目的自动辅助原理

### 3.1 DLL注入
通过 `SetWindowsHookEx` 安装 `GetMsgProc` 钩子，当游戏窗口创建消息到达时检测目标进程。

### 3.2 窗口子类化
```cpp
g_OldProc = (WNDPROC)GetWindowLongPtrA(hWnd, GWL_WNDPROC);
SetWindowLongPtrA(hWnd, GWL_WNDPROC, (LONG_PTR)NewWndProc);
```
替换窗口过程，拦截自定义 WM_CGA_* 消息来执行游戏操作。

### 3.3 API Hook (Detours)
Hook了 CreateMutexA, RegisterHotKey, SetActiveWindow, Sleep, CreateProcessA 等API。

### 3.4 游戏数据访问
通过 `g_playerBase` 指针(全局变量)访问堆上的 `playerbase_t` 结构，使用 `CXorValue`(异或加密)存储HP/MP等关键数值。

### 3.5 自动化操作
几乎所有操作最终都是发送网络封包：
- 普通攻击: "H|目标ID"
- 技能攻击: "S|技能位|等级|目标"
- 防御: "G"
- 逃跑: "E"
- 聊天: "P|消息"
- 移动: 方向+坐标封包

## 四、最安全的自动化方案（推荐）

### 核心结论：三层架构，零注入

| 层次 | 技术 | 用途 | 检测风险 |
|------|------|------|---------|
| 数据读取 | 外部 ReadProcessMemory | 读取HP/坐标/战斗状态等 | 几乎为零 |
| 操作执行 | 本地网络代理注入封包 | 战斗/移动/聊天/物品操作 | 几乎为零 |
| UI交互 | SendMessage 模拟点击 | NPC对话/购买/出售/采集 | 几乎为零 |

### 网络代理方案
```
游戏客户端 <-> 本地代理(127.0.0.1) <-> 游戏服务器
                    ^
              自动化脚本注入封包
```

## 五、实际尝试过程（重要！）

### 5.1 目标
用户的目标是在自己的游戏中（20多年前的编译后代码，无法修改源码）实现自动化辅助，
要求最不容易被检测到。选择了"外部ReadProcessMemory"方案。

### 5.2 游戏环境
- 游戏可执行文件: `cg_se_3000.exe`
- 进程PID: 25976（示例）
- 窗口类名: "魔力宝贝"（GBK编码）
- EXE基址: 0x400000
- 镜像大小: 0x103B000 (约16MB)
- 角色名: 休闲小药娘

### 5.3 遇到的问题和解决过程

#### 问题1: OpenProcess 返回 Error=5 (ACCESS_DENIED)
**原因**: 即使以管理员身份运行，Windows默认不启用 SeDebugPrivilege。
**解决**: 添加 `enable_debug_privilege()` 函数，调用 `AdjustTokenPrivileges` 启用 `SeDebugPrivilege`。

关键代码:
```python
class LUID(ctypes.Structure):
    _fields_ = [("LowPart", wintypes.DWORD), ("HighPart", wintypes.LONG)]
class LUID_AND_ATTRIBUTES(ctypes.Structure):
    _fields_ = [("Luid", LUID), ("Attributes", wintypes.DWORD)]
class TOKEN_PRIVILEGES(ctypes.Structure):
    _fields_ = [("PrivilegeCount", wintypes.DWORD), ("Privileges", LUID_AND_ATTRIBUTES * 1)]

def enable_debug_privilege():
    hToken = wintypes.HANDLE()
    OpenProcessToken(GetCurrentProcess(), TOKEN_ADJUST_PRIVILEGES | TOKEN_QUERY, ctypes.byref(hToken))
    luid = LUID()
    LookupPrivilegeValueA(None, b"SeDebugPrivilege", ctypes.byref(luid))
    tp = TOKEN_PRIVILEGES()
    tp.PrivilegeCount = 1
    tp.Privileges[0].Luid = luid
    tp.Privileges[0].Attributes = SE_PRIVILEGE_ENABLED
    AdjustTokenPrivileges(hToken, False, ctypes.byref(tp), 0, None, None)
    CloseHandle(hToken)
```

注意: Python ctypes 中 `LUID_AND_ATTRIBUTES` 不在 `wintypes` 中，必须自定义。定义顺序必须是 LUID -> LUID_AND_ATTRIBUTES -> TOKEN_PRIVILEGES。

#### 问题2: IsInGame 判断为"未进入游戏"，但实际已进入
**原因**: 源码中的偏移量与用户游戏版本不匹配。
**证据**:
- `g_is_ingame (0xA15190)` 读取值为 0
- `g_player_name (0xA150B0)` 读取值为空字符串
- `g_playerBase** (0xCAEF88)` 指针值为 0x0
- `g_world_status XOR (0xC0C350)` 解码值为 0

**结论**: 用户的 `cg_se_3000.exe` 是不同的编译版本，所有全局变量偏移都不同。

#### 问题3: 内存搜索角色名找到了位置，但按 playerbase_t 结构解读数据不对
**搜索结果**: 角色名 "休闲小药娘" 出现在4处:
- 0x746E21 (偏移 0x346E21)
- 0x75D43A (偏移 0x35D43A)
- 0xED4028 (偏移 0xAD4028)
- 0x1074CF8 (偏移 0xC74CF8)

**分析**:
- 0x746E21: 游戏内部UI/聊天相关数据
- 0x75D43A: 类似
- 0xED4028: 包含IP地址 "218.1.220.102"，是网络连接结构，不是玩家数据
- 0x1074CF8: 包含 level=4，但HP/MP不在附近

#### 问题4: 在EXE镜像范围内搜索不到HP/MP值
**原因**: 用户的游戏版本中，HP/MP等关键数值**不在EXE镜像的全局变量中**，而是存储在堆内存上，通过指针间接访问。而且**没有使用XOR加密**（在整个镜像范围内搜索XOR编码值，未找到任何匹配）。

**关键发现**:
- 结构A (0xED3D80): 包含IP地址，是网络结构，level=3出现在这里可能是巧合
- 结构B (0x1074CF8): 名字在0x1074CF8，level=4在0x1074E9C(距名字+0x1A4)
  - 但名字到level之间全是0，没有HP/MP
  - level后面也全是0
  - 附近有 "CCA" 字符串和值 242200

**推断**: 用户的游戏版本中，玩家数据结构(playerbase_t)分配在堆上，通过一个全局指针间接访问。结构B(0x1074C00)可能是某种全局状态结构，包含名字和等级的副本，但HP/MP在另一个完全不同的堆结构中。

### 5.4 当前状态
已创建交互式内存搜索器(scanner.py)，类似金山游侠/Cheat Engine，支持:
- 首次全内存搜索(int/short/XOR)
- 再次搜索(在已有结果中过滤)
- 搜索"未变化"/"已变化"
- 保存/加载搜索结果
- 查看地址详情

### 5.5 下一步需要做的事
1. **使用scanner.py定位HP地址**:
   - 记下当前HP值，首次搜索
   - 在游戏中改变HP，再次搜索新值
   - 重复直到只剩1-2个地址
2. **同样方式定位 MaxHP, MP, MaxMP, 等级, 金币, 受伤程度 等地址**
3. **分析这些地址之间的偏移关系**，推断出完整的结构布局
4. **搜索指向这些地址的全局指针**，找到 g_playerBase 的偏移
5. **基于确定的偏移，编写最终的读取脚本**

## 六、源码中的关键偏移（仅供参考，与用户版本不匹配）

### cg_se_3000 版本偏移 (gameservice.cpp Initialize函数)
```cpp
// 这些偏移对用户的游戏版本无效，仅作参考
g_playerBase = CONVERT_GAMEVAR(playerbase_t **, 0xCAEF88);  // 指针的指针
g_is_ingame = CONVERT_GAMEVAR(int *, 0xA15190);
g_player_name = CONVERT_GAMEVAR(char *, 0xA150B0);
g_world_status = CONVERT_GAMEVAR(CXorValue *, 0xC0C350);
g_game_status = CONVERT_GAMEVAR(CXorValue *, 0xC0C360);
g_player_xpos = CONVERT_GAMEVAR(CXorValue *, 0x8B03B4);
g_player_ypos = CONVERT_GAMEVAR(CXorValue *, 0x8B03E4);
```

### cg_item_6000 版本偏移
```cpp
g_playerBase_cgitem = CONVERT_GAMEVAR(playerbase_t *, 0xE12C30);  // 直接指针
g_is_ingame = CONVERT_GAMEVAR(int *, 0xBDBA78);
g_player_name = CONVERT_GAMEVAR(char *, 0xBDB998);
g_world_status = CONVERT_GAMEVAR(int *, 0xE1E000);  // 非XOR
g_game_status = CONVERT_GAMEVAR(int *, 0xE1DFFC);   // 非XOR
```

### playerbase_t 结构布局 (gameservice.h)
```cpp
// sizeof = 很大，包含物品栏
offset  0: short race
offset  4: int level
offset 24: CXorValue hp        // 16 bytes: unknown(4) + key1(4) + key2(4) + refcount(1) + pad(3)
offset 40: CXorValue maxhp
offset 56: CXorValue mp
offset 72: CXorValue maxmp
offset 236: int health          // 受伤程度 0=健康
offset 240: int souls
offset 244: int gold
offset 256: char name[17]
offset 380: int direction
offset 412: item_info_t items[] // 装备8格 + 背包20格

// item_info_t sizeof = 0x65C (1628 bytes)
offset   0: short valid
offset   2: char name[46]
offset 1592: int image_id
offset 1596: int level
offset 1600: int item_id
offset 1604: int count
offset 1608: int type
offset 1620: int assessed
```

### CXorValue 解码方式
```cpp
struct CXorValue {
    int unknown;
    int key1;
    int key2;
    unsigned char refcount;
    // padding 3 bytes
    int decode() { return key1 ^ key2; }
};
```

**重要**: 用户的游戏版本很可能**不使用XOR加密**，因为在整个EXE镜像中搜索XOR编码的HP/MP值，未找到任何匹配。

## 七、已创建的脚本文件

| 文件 | 功能 | 状态 |
|------|------|------|
| external_reader_test.py | 外部读取测试脚本(使用已知偏移) | 偏移不匹配，无法使用 |
| diag.py | 诊断脚本，检查已知偏移+搜索角色名 | 已运行，确认偏移不匹配 |
| search.py | 按数值搜索playerbase | 已运行，找到候选但数据不对 |
| probe.py | 以角色名为锚点探测结构 | 已运行，发现HP/MP不在名字附近 |
| dump.py | 大范围dump+搜索已知值 | 已运行，确认HP/MP不在EXE镜像中 |
| memscan.py | 全内存搜索HP+MaxHP+MP+MaxMP组合 | 有bug已修复，未成功运行 |
| scanner.py | 交互式内存搜索器(类金山游侠) | 已创建，待使用 |

## 八、Python ctypes 编程踩坑记录

### 8.1 LUID_AND_ATTRIBUTES 不在 wintypes 中
必须自定义: LUID -> LUID_AND_ATTRIBUTES -> TOKEN_PRIVILEGES，且定义顺序必须正确。

### 8.2 f-string 中不能有中文引号
```python
# 错误
print(f"搜索"未变化"")
# 正确
print(f"搜索[未变化]")
```

### 8.3 f-string 格式化表达式
```python
# 错误
f"0x{val:X if val else 0}"
# 正确
f"0x{val if val else 0:X}"
```

### 8.4 c_void_p 返回 None
`MEMORY_BASIC_INFORMATION.BaseAddress` 是 `c_void_p`，当地址为0时Python返回None。
```python
base = mbi.BaseAddress if mbi.BaseAddress is not None else addr
```

### 8.5 EnumProcessModules 权限
只用 PROCESS_VM_READ 权限时 EnumProcessModules 可能失败，需要降级处理，使用默认基址 0x400000。

## 九、Windows API 使用要点

### 9.1 进程访问
```python
PROCESS_VM_READ = 0x0010
PROCESS_QUERY_INFORMATION = 0x0400
handle = OpenProcess(PROCESS_VM_READ | PROCESS_QUERY_INFORMATION, False, pid)
```

### 9.2 内存区域枚举
```python
MEMORY_BASIC_INFORMATION 结构
VirtualQueryEx 枚举进程内存区域
MEM_COMMIT (0x1000) = 已提交的内存
PAGE_READWRITE (0x04) 等可读保护标志
```

### 9.3 ReadProcessMemory
```python
buf = ctypes.create_string_buffer(size)
bytesRead = ctypes.c_size_t()
ReadProcessMemory(handle, address, buf, size, ctypes.byref(bytesRead))
```

## 十、给下一个AI的具体任务

### 任务1: 使用scanner.py定位所有关键地址
让用户运行scanner.py，通过多次搜索定位以下值的内存地址:
- 当前HP
- 最大HP
- 当前MP
- 最大MP
- 等级
- 金币
- 受伤程度(health)
- 角色名

### 任务2: 分析地址之间的偏移关系
找到所有地址后，计算它们之间的偏移差，推断出完整的结构布局。
特别关注:
- HP和MaxHP之间的偏移差(原版是16字节，因为CXorValue占16字节)
- 如果用户版本不用XOR，HP和MaxHP可能只差4字节
- 名字和HP之间的偏移差

### 任务3: 找到全局指针
在EXE镜像的.data段中搜索指向playerbase结构的指针，找到 g_playerBase 的偏移。
方法: 在0x400000~0x400000+0x103B000范围内搜索指向playerbase起始地址的4字节指针。

### 任务4: 编写最终的读取脚本
基于确定的偏移，编写一个完整的Python脚本，实现:
- 读取HP/MP/等级/金币/受伤程度
- 读取物品栏20格物品名称和数量
- 实时刷新显示

### 任务5: 实现自动化操作
按照"最安全自动化方案.md"中的三层架构:
1. 外部ReadProcessMemory读取数据
2. 网络代理注入封包执行操作
3. SendMessage模拟UI交互
